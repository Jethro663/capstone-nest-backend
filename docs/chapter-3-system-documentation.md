# Chapter 3 System Documentation Pack

Prepared from the current Nexora codebase and deployment topology as of March 22, 2026.

## Table 1. Software Requirements

| Software | Specification |
| --- | --- |
| Windows Operating System | Windows 10 64-bit or Windows 11 64-bit is recommended as the primary development operating system for the Nexora project. It supports Docker Desktop, Visual Studio Code or WebStorm, Node.js, Python, PostgreSQL tooling, and Android or Expo-based mobile testing workflows. |
| Windows Subsystem for Linux (WSL 2) | WSL 2 is recommended for Linux-compatible command-line tooling, container support, and script execution during development and deployment preparation. A current Ubuntu-based WSL 2 environment with virtualization enabled is sufficient. |
| Web Browser | A modern Chromium-based browser such as Google Chrome or Microsoft Edge, or the latest Mozilla Firefox release, is required for accessing the web application, testing responsive behavior, opening the Swagger API documentation, and validating the generated system architecture output. |
| Visual Studio Code / JetBrains WebStorm | A modern TypeScript-capable integrated development environment is required for maintaining the NestJS backend, Next.js frontend, and Expo mobile application. Visual Studio Code and JetBrains WebStorm are both suitable because they support TypeScript, ESLint, terminal integration, and debugging. |
| Git | Git is required for source code version control, branch management, and collaborative development of the Nexora repository. Any current Git for Windows release is sufficient. |
| Node.js | Node.js version 18 or higher is required to run the backend and frontend toolchains. The current codebase uses NestJS, Next.js 16, React 19, Tailwind CSS, testing tools, and build scripts that depend on an up-to-date Node.js runtime. |
| npm | npm is required as the package manager for installing and running the JavaScript and TypeScript dependencies of the backend, frontend, and mobile workspaces. A version bundled with modern Node.js releases is sufficient. |
| Python | Python 3.11 or higher is recommended for the FastAPI-based AI service. It is used for AI mentor chat orchestration, PDF extraction, retrieval and indexing workflows, and related dependency management through `pip`. |
| Docker Desktop | Docker Desktop is recommended for running the integrated local stack defined in `docker-compose.yml`, including the frontend, backend, PostgreSQL with pgvector, Redis, Ollama, and the Python AI service. It is also useful for school or pre-production deployment rehearsal. |
| pgAdmin 4 | pgAdmin 4 is recommended for database inspection, query execution, schema verification, and monitoring the PostgreSQL database used by Nexora. It is suitable for validating tables, intervention data, assessment records, and AI-related vectorized content. |
| Android Studio | Android Studio is recommended for Android emulator-based testing of the mobile application, especially when validating student-facing screens, authentication, assessments, lessons, LXP access, and profile workflows. |
| Expo Go / Expo CLI | Expo tooling is required for running and testing the mobile application built with Expo SDK 54 and React Native. It supports rapid testing on physical Android devices and simplifies the execution of the mobile student module. |
| Swagger UI | Swagger UI is included in the NestJS backend for API documentation and endpoint verification. It is required for inspecting authentication, class, lesson, assessment, intervention, analytics, and reporting endpoints during testing and technical validation. |
| Next.js | Next.js 16.1.6 is the primary web application framework for Nexora. It is used to build the administrator, teacher, and student web dashboards, authentication pages, responsive UI routing, and browser-delivered views. |
| React | React 19 powers the component-based user interface of the web application and also supports the mobile application through React Native. It is required for rendering dashboards, forms, class views, notifications, reports, and AI-related user interactions. |
| Tailwind CSS | Tailwind CSS is used in the web frontend for utility-based styling, layout control, responsive behavior, and consistent interface presentation across the dashboard pages. |
| NestJS | NestJS 11 is the primary backend framework of Nexora. It manages REST APIs, authentication, role enforcement, class and lesson workflows, assessments, LXP logic, reporting, notifications, audit logging, and system integration with AI services. |
| FastAPI | FastAPI is used by the separate Python AI microservice that handles JAKIPIR mentor chat, PDF extraction, retrieval and indexing pipelines, remedial generation, and AI-assisted instructional support. |
| PostgreSQL with pgvector | PostgreSQL serves as the main relational database for user records, classes, lessons, assessments, performance logs, intervention records, reports, and audit data. The current Docker deployment uses a `pgvector/pgvector:pg16` image so that embeddings and retrieval-ready vectors can be stored for AI-assisted search and indexing. |
| Redis | Redis 7 is used as the in-memory message broker and queue backend for background processing. It supports asynchronous jobs and service coordination for parts of the backend that rely on BullMQ. |
| BullMQ | BullMQ is used in the NestJS backend to process queued tasks such as notifications, announcements, and retrieval or indexing-related jobs. It supports dependable background execution and reduces blocking operations in the main request cycle. |
| Drizzle ORM | Drizzle ORM is used as the database access and schema management layer for the backend. It supports strongly typed database interaction, migrations, and alignment between the PostgreSQL schema and backend modules. |
| Ollama | Ollama is the local model-serving runtime used by the AI service. The present stack uses `qwen2.5:3b` for text generation, `gemma3:4b` for vision and document-oriented reasoning, and `nomic-embed-text` for embedding generation and retrieval support. |
| Socket IO | Socket IO is used for real-time notifications between the backend and connected web clients. It supports live delivery of notification events and improves responsiveness for dashboard users. |
| JWT Authentication Stack | The system uses JSON Web Tokens with Passport and NestJS guards to secure authenticated API access across administrator, teacher, and student workflows. It supports access-token validation, refresh-token workflows, and route protection. |
| OpenTelemetry, Prometheus, Tempo, and Loki Integration | The repository includes OpenTelemetry instrumentation and monitoring configuration for Prometheus and Tempo, together with Loki-compatible logging support through Winston Loki. These tools are intended for observability, tracing, metrics collection, and operational diagnostics during deployment and maintenance. |

## Table 2. Hardware Specifications

| Hardware | Specification |
| --- | --- |
| Development Workstation (Developer or Thesis Implementation Unit) | A desktop or laptop computer with at least a 64-bit Intel Core i5 or AMD Ryzen 5 processor, 16 GB RAM, and 512 GB SSD storage is recommended for reliable local development. This baseline is suitable for running Windows, WSL 2, Docker Desktop, Node.js services, the Python AI service, database tools, and mobile development or testing utilities without severe slowdown. |
| School Administrator or Teacher Client Computer | For regular web-based operation, a 64-bit desktop or laptop with at least a dual-core 2.0 GHz processor, 8 GB RAM, 128 GB of available SSD or HDD storage, and a stable broadband connection is sufficient. This specification supports browser-based access to dashboards, classes, assessments, reports, notifications, and profile management. |
| Student Android Smartphone | A device with Android 10 or higher, at least 4 GB RAM, an octa-core mid-range processor, 64 GB internal storage, and stable Wi-Fi or mobile data connectivity is recommended. This allows the Expo-based mobile application to run student-facing modules such as dashboard access, assessments, lessons, LXP activities, announcements, performance viewing, and profile functions with acceptable responsiveness. |
| Core LMS School Server or Cloud VM (Without AI Model Hosting) | For hosting the web frontend, NestJS backend, PostgreSQL database, Redis, and shared upload storage without colocated AI inference, a server or virtual machine with at least 4 vCPU, 16 GB RAM, 200 GB SSD storage, and a Linux-based operating system such as Ubuntu Server 22.04 LTS is recommended. This is appropriate for pilot or school-scale deployment of the main LMS and reporting functions. |
| AI-Capable Server or Cloud VM for Practical Mid-Level Performance | To run Ollama together with the Python AI service at usable mid-level performance for chat, document extraction, and embedding workflows, a server or cloud VM with at least 8 vCPU, 32 GB RAM, 250 GB SSD storage, and an NVIDIA GPU with 16 GB VRAM such as an NVIDIA T4, RTX 4060 Ti 16 GB, or equivalent is recommended. This baseline is appropriate for the current `qwen2.5:3b`, `gemma3:4b`, and `nomic-embed-text` model mix and provides materially better response times than CPU-only hosting. |
| AI-Capable CPU-Only Fallback Server | If a GPU-backed instance is unavailable, a fallback server with at least 12 vCPU, 48 GB RAM, and fast SSD storage may still run the AI service, but response times for generation and document processing will be significantly slower. This option is not ideal for real-time classroom use, but it remains workable for light pilot operation or off-peak processing. |
| Network and Connectivity Infrastructure | A stable broadband connection and secure local network are required for browser access, mobile synchronization, API communication, notification delivery, and cloud-hosted deployment. For school use, dependable Wi-Fi coverage and at least a standard business-grade router or managed network environment are recommended to reduce service interruption. |

## Figure 1. System Architecture

The Nexora system follows a multi-tier web and mobile client-server architecture. The web layer is implemented through a Next.js 16 and React 19 frontend that serves administrator, teacher, and student dashboards. The mobile layer is implemented through an Expo and React Native student-facing application that currently includes authentication, dashboard, class, lesson, assessment, LXP, performance, announcement, and profile views.

Both client layers communicate with a NestJS 11 backend through authenticated HTTP-based API requests. The backend centralizes authentication, role and profile control, sections and class management, lessons, assessments, class records, announcements, notifications, reports, analytics, audit logging, teacher-facing intervention tools, and LMS to LXP coordination. The backend also exposes Swagger-based technical documentation and uses Socket IO for real-time notifications.

The backend stores operational data in PostgreSQL and relies on Redis with BullMQ for queued and asynchronous work. An event-driven application layer connects assessment submission, performance analysis, class-record synchronization, notification dispatch, and LXP intervention triggering. For AI-supported functions, the backend integrates with a separate FastAPI microservice responsible for JAKIPIR mentor chat, PDF extraction, indexing, retrieval, remedial support generation, and related inference workflows. That AI service uses Ollama-hosted text, vision, and embedding models, and stores relational or vector-supporting records through the shared PostgreSQL environment.

Planned or concept-paper-aligned scope that extends beyond the presently visible code paths remains centered on broader instructional support, system evaluation, and expanded intervention intelligence. These planned areas are included in the architecture representation only when clearly identified as part of the broader Nexora system scope.

The HTML version of the architecture figure is stored in `docs/chapter-3-system-architecture.html`.
