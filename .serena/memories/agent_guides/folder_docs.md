Folder-local agent guides exist at `backend/AGENTS.md`, `ai-service/AGENTS.md`, `test-mobile/AGENTS.md`, and `next-frontend/AGENTS.md`. Use them first when a task is scoped to one of those folders. `backend/AGENTS.md` covers NestJS module boundaries, Drizzle/schema/migration workflow, auth/RBAC, response envelope, and AI proxy boundaries. `ai-service/AGENTS.md` covers FastAPI internal-service constraints, forwarded-header/shared-secret contract, AI capability modules, and backend DB/proxy coupling. `test-mobile/AGENTS.md` covers Expo boot path, navigation/auth/API workflow, theme/style sources, and student-app constraints. `next-frontend/AGENTS.md` covers App Router structure, web auth/refresh flow, rewrite-based API access, role shells, and theme sources. Prefer these docs before deeper exploration for folder-scoped work; they are compact by design for repeated agent mentions.