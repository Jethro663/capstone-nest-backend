# Betamochi Legacy Notice

This folder is a legacy mobile-era target and is not the default Codex mobile app.

- Generic `mobile` or Expo requests should route to `test-mobile/`.
- Work in `betamochi/` only when the prompt explicitly names this folder or the files under it.
- Do not silently use `betamochi/` for current mobile work.
